# Em dev, deixe VAZIO ou nao crie o .env — o app usa '/api' (proxy do Vite).
# Em producao (Render Static Site), defina com a URL do backend:
#   VITE_API_URL=https://bovia-api.onrender.com
VITE_API_URL=
